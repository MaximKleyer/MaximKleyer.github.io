// Copy this file to `config.js` and fill in your own RapidAPI key.
//
// `config.js` is gitignored so the key never enters version control.
//
// IMPORTANT: this is a static site with no build step, so whatever key
// you put in config.js is downloaded by every visitor and is readable in
// devtools. Gitignoring it keeps it out of the repo and its history, but
// it does NOT make it secret once the site is deployed. If this page is
// ever public, put the key behind a small server-side proxy instead and
// have these scripts call the proxy.
window.APP_CONFIG = {
  RAPIDAPI_KEY: 'YOUR_RAPIDAPI_KEY_HERE',
  RAPIDAPI_HOST: 'free-football-live-score.p.rapidapi.com',
};
