# Local secrets — never commit. Copy config.example.js to config.js.
config.js

# Dependencies
node_modules/

# Build output
dist/
build/
# NOTE: gm-sim/ is deliberately NOT ignored. gm-sim-react/vite.config.js
# builds into ../gm-sim with base '/gm-sim/', i.e. the built app is meant
# to be committed and served from the site root like the other pages.

# Environment
.env
.env.local
.env.*.local

# Archives / large binaries
*.zip

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
